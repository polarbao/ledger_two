# v1.2 Release Hardening 任务计划

状态：本地实现、production 备份和 staging 文件准备已完成，等待 sudo 启动容器

## RC01 发布资料冻结

状态：已完成

1. 发布说明、升级说明和发布候选验收清单。
2. 版本 `1.2.0-rc`、schema 18 和 OpenAPI 口径统一。
3. 本机后端、前端、Compose、Docker 和 health 门禁通过。

## RC02 部署环境与数据库隔离

状态：本地实现完成；NAS production 一致性备份完成，原库仍为 schema 12

目标：

1. 增加 `DEPLOYMENT_CHANNEL` 并在 health、系统诊断和前端显示。
2. Compose 强制显式容器名、镜像标签和部署 channel。
3. NAS staging 与 production 使用不同目录、端口、容器、密钥和数据库。
4. 提供 SQLite 一致性预部署备份脚本与校验清单。

完成标准：

1. 非法 channel 拒绝启动。
2. staging 显示“验收环境”，production 显示“正式数据”。
3. staging migration 只修改数据库副本，production schema 12 原库保持不变。
4. 备份副本通过 `PRAGMA quick_check`，并记录 schema 与核心表数量。

## RC03 NAS staging 验收

状态：已完成；迁移、数据保留、登录、诊断、规则读取、导入预览、备份、390px 登录页和容器重启均通过

1. 在 `38089` 启动独立 staging。
2. 验证 health 为 `1.2.0-rc`、schema 18、channel staging。
3. 验证原有用户、账本、账单和结算数量在迁移前后保持一致。
4. 验证登录、历史账单、附件、导入预览、规则和备份。

当前结果：

1. health 为 `1.2.0-rc`、schema 18、channel staging。
2. migration 前后用户/账本/账单/结算保持 2/1/7/1。
3. Owner 登录、系统诊断、规则读取、4 行 CSV 预览和手动备份通过。
4. 临时 QA 密码已恢复，两个用户的密码 Hash 与升级前备份一致。
5. 390px 登录页环境标识可见、横向溢出 0、路由错误 0。

## RC04 NAS production 升级

状态：production 已升级到 schema 18；历史数据、环境标识和升级后备份通过，等待真实账号抽检和容器重启

1. 再次备份 production。
2. 使用已验收镜像升级 production 原库。
3. 验证 health 为 `1.2.0-rc`、schema 18、channel production。
4. 完成升级后备份、容器重启和回滚材料确认。

## RC05 真实微信账单导入准备

状态：输入格式阻塞，未同步

1. 待导入文件为 XLSX，当前 v1.2 仅支持 CSV，禁止直接上传或提交。
2. 文件只读检查确认第 18 行为完整微信表头，共 276 条数据记录。
3. 优先重新导出官方 CSV；若必须使用 XLSX，则先单独评审和实现无数据库写入的转换工具。
4. 真实账单必须先走 staging 预览、状态核对、抽样对账和用户确认，再进入 production 事务提交。
