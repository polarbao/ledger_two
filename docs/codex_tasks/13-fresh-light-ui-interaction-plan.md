# Task13：Fresh Light UI/UX 协同开发计划

状态：UI-FL-01 至 UI-FL-10 已完成，波次 A-E 与 Fresh Light UI/UX 专项已关闭<br>
创建日期：2026-07-13<br>
适用阶段：v1.2 发布收口后、v1.3 业务扩展前的体验质量专项；后续业务任务中的 UI 变更持续复用本计划

## 1. 目标

将 Fresh Light 从设计目标转成可执行、可并行但不互相覆盖的前端任务，同时解决两个问题：

1. 按页面逐步迁移现有 Dark Glass 界面，不一次性重写应用。
2. 让 UI 专项与业务 Task 同步推进，避免同一组件被不同任务重复设计、重复实现或改变冻结业务口径。

本计划是 UI-FL-01 至 UI-FL-10 的执行入口。设计细节以 `docs/ui/figma/ledger-two-fresh-light-implementation-spec-2026-07-13.md` 为准，业务、金额、权限和 API 仍以 PRD、技术契约、OpenAPI 和后端实现为准。

## 2. 当前状态

- Task41-Task49 已完成，其业务行为和 `Task47U/48U/49U` 验收事实不得被本专项重写。
- Task49X 的 XLSX 业务与发布收口继续由 `docs/codex_tasks/12-v1.2-xlsx-import-special-plan.md` 管理；`UI-FL-08` 只能在其契约上更新界面。
- Fresh Light 规范清单与本地审阅包均覆盖 29 个 Frame，三个页面简称已有显式映射。
- 本地 HTML、SVG、Manifest 是生成审阅证据，不等于线上 Figma 节点、Variables 或 Auto Layout 已完整同步。
- Dark Glass 是已验收历史基线和显式回滚主题，不再是无偏好新会话的默认体验。
- UI-FL-01 已建立 Fresh Light/Dark Glass 语义 Token、主题切换基础和共享基础组件；UI-FL-10 关闭后 Fresh Light 已成为全应用默认主题。
- UI-FL-02 已完成 AppShell、五项一级导航、工具入口、账本/角色、网络/草稿状态和移动记账 FAB 的双主题迁移；页面内容仍按后续 UI-FL 任务逐页迁移。
- UI-FL-03 已完成 Dashboard 月度摘要、结算行动、周期提醒、分类/成员摘要和最近流水迁移；快捷记账继续只调用全局抽屉入口，表单编排留给 UI-FL-04。
- UI-FL-04 已完成记账抽屉的金额优先、高低频分层、共同支出承担预览、脏字段关闭确认和桌面/移动 Footer；API、默认值、复制、模板、草稿、附件和 mutation 契约保持不变。
- 2026-07-14 已增加全局主题切换入口与本地偏好持久化，登录、初始化、桌面和移动端均可显式切换 Fresh Light/Dark Glass；2026-07-15 UI-FL-10 验收后，无偏好新会话默认值已翻转为 Fresh Light，既有显式 Dark Glass 偏好继续保留。
- 2026-07-15 UI-FL-05E 已在本机固定镜像使用真实 QA 账号完成 390/1440px 运行回归，并在隔离数据库副本验证普通/共同编辑、归档元数据、附件引用、批量标签、软删除和 CSV 导出；原本机 staging 数据保持不变。
- 2026-07-15 UI-FL-06 已完成结算范围、解释字段、复制兜底和生成确认迁移；本机 Fresh Light 375/1440px、真实账号只读路径及隔离数据库结算写路径通过，原 staging 保持 40 条流水和 2 条结算记录。
- 2026-07-15 UI-FL-07 已完成设置六分区、成员角色态、元数据生命周期、备份恢复和脱敏诊断迁移；本机 Fresh Light 375/1440px 无横向溢出，归档确认只读路径和全量权限回归通过，主 staging 数据未改变。
- 2026-07-15 UI-FL-08 已完成导入来源、Preview、状态筛选、桌面表格、移动卡片、行编辑、规则管理和原子提交确认迁移；本机 Fresh Light 375/1440px 无横向溢出，匿名 fixture 仅新增预览批次，主 staging 仍为 40 条正式流水。
- 2026-07-15 UI-FL-09 已完成近六个月趋势、分类/标签排行、成员支付与承担及流水钻取迁移；375/1440px 无横向溢出，分类/成员/标签钻取保持现有 URL 契约，运行验收没有产生数据库写入。
- 2026-07-15 UI-FL-10 已完成全局主题、可访问性、响应式和真实路径收口；登录及七个业务路由在 375/390/430/1440 共 28 个组合通过，Dark Glass 回退有效，数据库计数保持不变。

## 3. 范围

### 3.1 包含

- Fresh Light 语义 Token、基础组件和双主题映射。
- AppShell、Dashboard、记账抽屉、流水、结算、设置、导入和分析页面的分阶段迁移。
- 375/390/430/1440 视口、可访问性和真实业务路径验收。
- 设计稿、任务文档、代码、截图和验收记录的同步更新。
- 后续业务任务涉及 UI 时的组件归属和冲突协调机制。

### 3.2 不包含

- 修改金额、分摊、结算、权限、导入去重、备份或审计规则。
- 未经独立评审新增 API、migration 或业务状态。
- 银行同步、OCR、复杂预算、原生 App 和直接通知共同支付。
- 为了视觉统一删除已有功能，或把所有页面一次性替换。

## 4. 事实源与冲突优先级

发生冲突时按以下顺序处理：

1. 已冻结 PRD、金额/权限/隐私规则。
2. 技术契约、OpenAPI、migration 和后端服务行为。
3. 当前 Task 规格与已完成验收记录。
4. Fresh Light 实施规格、Token、Variables、组件库和 Frame Manifest。
5. 在线 Figma Frame、截图和本地生成审阅包。

生成预览只能提出差异，不能反向修改业务规则。确需改变业务或 API 时，必须退出 UI-FL 任务，另开 PRD/DEV 评审和提交。

## 5. 与既有任务的映射

| UI-FL 任务 | 复用的业务任务 | 主要路由/模块 | 协同边界 |
|---|---|---|---|
| UI-FL-01 | Task36、Task46 | 全局样式、基础 UI | 只建立 Token 和基础组件，不改页面业务 |
| UI-FL-02 | Task36、Task46 | `AppShell`、一级导航 | 保留 active ledger、离线、草稿、RBAC 和 query 失效行为 |
| UI-FL-03 | Task41、Task43 | `/` Dashboard | 复用快捷记账和周期待确认，不新增聚合口径 |
| UI-FL-04 | Task41、Task42、Task46 | `TransactionFormDrawer` | 保留默认值、复制、模板、草稿和 mutation 行为 |
| UI-FL-05 | Task44、Task46 | `/transactions` | 复用筛选、元数据生命周期和软删除，不改查询语义 |
| UI-FL-06 | Task45 | `/settlement` | 后端继续是 paid/share/net 的权威来源，不改历史账单 |
| UI-FL-07 | Task37、Task40、Task44 | `/settings` 及元数据页 | 保留 owner/editor/viewer、备份、诊断和归档规则 |
| UI-FL-08 | Task47U、Task48U、Task49U、Task49XU | `/import`、`features/imports` | 不改 parser、hash、批次状态、commit 事务和规则优先级 |
| UI-FL-09 | 现有分析模块与统计契约 | `/analytics` | settlement 不进入消费统计，成员口径必须复用服务端数据 |
| UI-FL-10 | Task46 及全部 UI-FL | 全应用 | 只做跨页验收和缺陷收口，不借机扩业务范围 |

## 6. 共享组件归属

| 归属任务 | 唯一负责范围 | 其他任务使用规则 |
|---|---|---|
| UI-FL-01 | Token、按钮、Chip、Segment、Dialog、Sheet、焦点和状态语义 | 页面任务只能组合和提出缺口，不得创建局部颜色、圆角或同名组件 |
| UI-FL-02 | AppShell、导航、断点、移动 FAB、全局状态入口 | 页面任务不得自行复制导航或定义新全局断点 |
| UI-FL-04 | 表单字段分组、表单区块和 Footer 编排 | 模板、复制和页面入口复用同一表单 orchestration |
| UI-FL-05 | `ResponsiveDataList`、筛选栏、筛选 Chip 和移动列表外壳 | 导入、元数据等工作台优先复用，不另建近似实现 |
| UI-FL-10 | 跨页视觉、可访问性、回归和证据汇总 | 只登记缺陷并回派原归属任务，不直接重写业务模块 |

共享组件的 API 或视觉状态尚未冻结时，不允许两个页面任务同时修改该组件。确需并行时，先由归属任务提交基础契约，页面任务再基于该提交开发。

## 7. 同步协调机制

### 7.1 双向登记

任何业务 Task 触及前端时，任务文档必须增加或确认以下信息：

- 关联的 UI-FL 编号；没有关联时写明原因。
- 涉及路由、页面、共享组件和文件所有者。
- API、权限、金额和状态机是否保持不变。
- 与其他进行中任务是否存在同文件或同组件冲突。
- 需要同步的 Figma handoff、Frame、截图和验收记录。

UI-FL 任务启动时也必须反向列出所复用的历史/当前业务 Task，禁止只按截图改代码。

### 7.2 四个同步点

1. **任务启动**：冻结文件范围、共享组件归属、依赖和验收视口。
2. **契约冻结**：确认组件 props、状态、响应式结构和 API 不变声明。
3. **实现完成**：同一提交或同一 Task 内同步代码、任务状态和必要 handoff。
4. **验收完成**：补截图/CDP、真实业务路径、缺陷结论和回滚结果。

### 7.3 并行条件

允许并行必须同时满足：

- 不修改同一个共享组件，或组件归属任务已先冻结契约。
- 不修改同一路由的同一交互状态。
- 不需要互相等待未冻结 API、DTO 或业务规则。
- 每个任务有独立测试和截图范围。

不满足时按“基础组件 -> 页面骨架 -> 业务状态 -> 验收”顺序串行。业务缺陷优先于视觉迁移；发布阻断缺陷可暂停对应 UI-FL 波次。

## 8. 准入门槛

开始代码迁移前必须满足：

- v1.2 业务范围和 Task49X 发布边界已冻结；未完成项有明确归属，不混入 UI-FL。
- 工作树有可追踪提交基线，真实账单、数据库、密钥和原始 Figma 包未进入 Git。
- 当前应用关键页面已保留 Dark Glass 基线截图或等价验收记录。
- Frame Manifest 与本地预览清单 29/29 对齐。
- Fresh Light Token、组件状态和目标页面有本地规范；线上 Figma 未同步项明确标注。
- 主题切换或提交级回退路径明确，不能以删除 Dark Glass 作为迁移前提。

## 9. 执行波次

| 波次 | 任务 | 进入条件 | 完成标准 |
|---|---|---|---|
| A 基础 | UI-FL-01、UI-FL-02 | 本计划和设计基线冻结 | Token、基础组件、AppShell 和断点契约稳定 |
| B 高频 | UI-FL-03、UI-FL-04、UI-FL-05 | 波次 A 完成 | 首页、记账、流水的桌面/移动主路径可用 |
| C 信任 | UI-FL-06、UI-FL-07 | 所需共享组件稳定 | 结算、设置、元数据和高风险操作解释清楚 |
| D 工作台 | UI-FL-08、UI-FL-09 | Task49X 边界冻结、列表外壳稳定 | 导入和分析完成视觉迁移且业务口径不变 |
| E 收口 | UI-FL-10 | UI-FL-01 至 09 完成 | 全视口、可访问性和真实业务回归通过 |

波次 B 内建议先完成 UI-FL-04 的表单契约，再并行 UI-FL-03 与 UI-FL-05。波次 C 可并行。UI-FL-08 必须在 Task49X 的 UI 和契约完成验收后开始。

### 9.1 协同状态看板

| 任务 | 设计/规范 | 代码 | 验收 | 当前依赖 |
|---|---|---|---|---|
| UI-FL-01 | 已冻结 | 已完成 | 已完成 | 无 |
| UI-FL-02 | 已冻结 | 已完成 | 已完成 | 无 |
| UI-FL-03 | 已冻结 | 已完成 | 已完成 | 无 |
| UI-FL-04 | 已冻结 | 已完成 | 已完成 | 无 |
| UI-FL-05 | 已冻结 | 已完成 | 已完成 | 固定镜像、真实账号和隔离副本写路径均通过 |
| UI-FL-06 | 已冻结 | 已完成 | 已完成 | 全部/本月范围、复制兜底、确认和隔离写路径通过 |
| UI-FL-07 | 已同步 | 已完成 | 已完成 | 代码 `912523c`；本机双端与全量回归通过，Task50 仍未改设置页 |
| UI-FL-08 | 已冻结 | 已完成 | 已完成 | 代码 `c3399df`；本机双端、Owner 权限与 Preview 不写账门禁通过 |
| UI-FL-09 | 已冻结 | 已完成 | 已完成 | 代码 `9d8066f`；统计口径、三类钻取与本机双端门禁通过 |
| UI-FL-10 | 已冻结 | 已完成 | 已完成 | 代码 `c8d63fe`；默认主题、Dark 回退、焦点、28 个路由视口和数据守恒通过 |

状态只允许使用“未开始 / 进行中 / 已完成 / 阻塞”。每次任务启动、代码提交和验收收口时更新对应行，并同步当前业务 Task 或专项文档；不得仅在聊天中更新状态。

## 10. 原子任务

### UI-FL-01：Token 与基础组件

范围：语义 Token、Fresh Light/Dark Glass 映射、按钮、状态 Chip、Segment、Dialog、Sheet、空/错/权限状态和焦点样式。<br>
完成标准：页面不再新增局部主题常量；状态不只靠颜色；Dark Glass 仍可回退。<br>
验证：组件测试、lint、build、组件矩阵截图。

实现与验收（2026-07-13）：

- 主题入口：`frontend/src/theme/theme.ts`、`frontend/src/styles/tokens.css`、`frontend/src/styles/ui-primitives.css`。
- 共享组件：`Button`、`StatusChip`、`SegmentedControl`、`StatePanel`、`ConfirmDialog`、`BottomSheet`，并将既有 Empty/Error/Loading/Skeleton 状态接入语义样式。
- 回退策略：应用默认仍解析为 `dark-glass`；既有 CSS 变量通过兼容别名映射，未在本任务中切换页面默认视觉。
- 自动化结果：前端 10 个测试文件、36 个测试通过，lint 与生产构建通过；构建仍保留既有约 655 kB 主包告警，归属后续性能任务。
- 视觉证据：`docs/project_analysis/ui-fl-01-foundation-2026-07-13/`，含 Fresh Light、Dark Glass、危险确认框和 390px Bottom Sheet；390px CDP 指标为 `innerWidth=390`、`scrollWidth=390`。
- 边界：本次没有修改 API、DTO、权限、金额、migration 或业务状态机；页面级 Fresh Light 验收从 UI-FL-02 开始。

### UI-FL-02：AppShell 与全局导航

范围：桌面侧栏、移动底栏、账本选择、同步/草稿状态和记账 FAB。<br>
完成标准：五个一级导航稳定；375px 无遮挡；切换账本、离线和权限行为不变。<br>
验证：导航测试、375/390/1440 截图、账本切换与草稿入口回归。

实现与验收（2026-07-13）：

- 导航契约：一级导航固定为首页、流水、分析、结算、设置；导入和周期规则收纳到桌面工具区，设置子路由继续保持设置项激活。
- 账本与权限：保留 active ledger、全量 query 失效和角色同步；owner/editor 可记账，viewer 显示只读角色并禁用桌面入口和移动 FAB。
- 状态入口：保留浏览器在线/离线监听、离线草稿入口、全局记账抽屉和草稿抽屉；在线状态只表达网络可用，不虚构服务端已同步。
- 响应式：桌面使用 248px 固定侧栏；移动使用顶部账本/月度上下文、五项底栏和独立记账动作区，FAB 与页面内容、底栏无覆盖。
- 自动化结果：前端 11 个测试文件、41 个测试通过，lint 与生产构建通过；主包约 659 kB 的既有分包告警继续由后续性能任务处理。
- 视觉证据：`docs/project_analysis/ui-fl-02-app-shell-2026-07-13/`，覆盖 Fresh Light、Dark Glass、草稿、离线、viewer、375/390/1440；CDP `scrollWidth` 与视口宽度一致。
- 边界：组件预览使用实际 AppShell 与确定性 mock API/本地状态，不等于真实后端登录 E2E；本任务没有修改 API、DTO、金额、权限规则、migration 或业务状态机。

### UI-FL-03：Dashboard

范围：月度摘要、结算行动、周期提醒、最近流水和分类摘要。<br>
完成标准：5 秒内找到记账和待结算金额；不显示 UUID 或开发型文案。<br>
验证：Dashboard 测试、周期确认/跳过回归、375/1440 截图。

实现与验收（2026-07-13）：

- 信息层级：首屏固定为月份/记账入口、五项月度摘要、明确的“谁转给谁”结算行动；分类支出、成员支付/承担和最近流水继续使用既有 Dashboard 聚合数据。
- 结算语义：只读取服务端 `shared_balance`，使用成员展示名而非 UUID；进入 `/settlement` 查看或登记，文案明确结算新增独立记录且不改写历史共同账单。
- 周期行为：保留 Task43 的待确认、确认记账和跳过本期 mutation 及 query 失效范围，没有新增 reminder 状态或 API。
- 记账边界：首页按钮和移动 FAB 继续调用 `setAddDrawerOpen(true)`；没有提前修改 UI-FL-04 负责的表单字段、默认值、模板、复制或草稿编排。
- 响应式：桌面使用五列摘要和双列内容区；375px 摘要改为两列且最后一项跨列，避免空白网格；CDP 指标为 `innerWidth=scrollWidth=375`，无横向滚动。
- 自动化结果：前端 14 个测试文件、51 个测试通过，lint 与生产构建通过；主包约 661 kB 的既有分包告警继续归属性能专项。
- 视觉证据：`docs/project_analysis/ui-fl-03-dashboard-2026-07-13/`，包含 375/1440 Fresh Light 截图和可复核指标；截图使用真实 AppShell/Dashboard 组件与确定性预览数据，临时预览入口未保留。
- 边界：本任务没有修改 API、DTO、金额、权限规则、migration、结算算法或业务状态机；真实后端登录、周期确认/跳过写入和跨页回归继续纳入 UI-FL-10 E2E。

### UI-FL-04：记账抽屉

范围：金额优先、高低频字段分层、共同支出预览、固定 Footer 和移动 Bottom Sheet。<br>
完成标准：普通支出 10 秒、共同支出 20 秒；保存并继续无脏字段继承。<br>
验证：表单纯函数和组件测试、普通/共同/复制/模板/草稿真实路径、375/390/1440 截图。

启动前契约（2026-07-13）：

- 准入结论：Task41/42/46 行为、Fresh Light Token、AppShell 入口和 UI-FL-03 快捷记账边界均已稳定；无需新增 API、DTO、migration 或第三方依赖，可以直接进入代码阶段。
- 编排边界：`TransactionFormDrawer.tsx` 继续拥有 query、mutation、默认值、复制、模板、上传、草稿和 query invalidation；拆出组件只能接收值和回调，不直接请求 API。
- 展开规则：金额、类型、分类、账户、日期默认展开；共同支出显示付款人、分摊方式、参与人和承担预览；标题、标签、可见性、备注、附件和模板管理进入低频区。复制/草稿已有低频值或低频字段校验失败时自动展开。
- 关闭规则：`react-hook-form` reset 后视为干净；用户修改后通过关闭按钮、遮罩或 Escape 退出时统一使用 `ConfirmDialog`，成功提交、保存离线草稿和显式放弃后不重复确认；关闭后焦点返回原触发控件。
- Footer：桌面保留取消、保存并继续、保存账单；移动端以保存账单为全宽主动作，取消和保存并继续为次级动作。模板入口退出主 Footer，避免与提交动作竞争。
- 测试矩阵：普通支出默认值、共同支出 equal/payer_only、复制、模板、离线草稿、viewer、上传中/失败、脏字段关闭、保存并继续、提交失败；视口覆盖 375/390/430/1440。
- 详细准备记录：`docs/project_analysis/2026-07-13-ui-fl-04-readiness.md`。

实现与验收（2026-07-14）：

- 高频编排：金额置顶，类型、分类、账户和日期保持默认展开；标题、标签、可见性、备注、附件和模板归入“更多选项”，复制、草稿、模板低频值及低频字段校验失败时自动展开。
- 共同支出：付款人、`equal/payer_only`、参与状态和承担金额在提交前同屏预览；金额继续按整数分计算，均分余数归付款人，最终结果明确以服务端校验为准。
- 提交与关闭：Footer 复用统一 Button，保留保存并继续、复制另存、草稿正式提交和离线草稿文案；脏字段、上传中关闭统一进入危险确认，成功保存与显式放弃不重复确认。
- 可访问性：抽屉接入 `useModalSurface` 的 Escape、焦点圈定和焦点返回；关闭、清空金额、移除附件均有可读名称，危险确认复用 `ConfirmDialog`。
- 响应式：桌面固定 440px 右抽屉；1024px 以下切换为不高于 92dvh 的底部 Sheet；430px Footer 由两项次级动作和全宽主动作组成，375/390/430/1440 均无横向滚动。
- 自动化结果：前端 16 个测试文件、59 个测试通过，lint 与生产构建通过；主包约 664 kB 的既有分包告警继续归属性能专项。
- 视觉证据：`docs/project_analysis/ui-fl-04-transaction-drawer-2026-07-14/`，覆盖桌面、共同支出、更多选项和脏字段放弃确认；使用真实组件与确定性查询响应，不替代真实后端写入 E2E。
- 边界：未新增依赖、API、DTO 或 migration，未修改普通/共同账单 payload、权限、默认值缓存、模板生命周期、附件接口或 query invalidation 范围。

### UI-FL-05：流水工作台

范围：桌面表格、移动卡片、搜索、筛选、详情和行操作。<br>
完成标准：移动端无宽表；历史归档名称、可见性和共同/个人均有文字。<br>
验证：筛选与软删除回归、长标题和空状态、375/390/1440 截图。

阶段实现与验收（2026-07-14）：

- 共享契约：新增 `ResponsiveDataList` 与 `ActiveFilterChips`，桌面/移动区域使用同一数据和事件，不复制查询或业务状态；UI-FL-08/09 可在该契约上复用。
- 工作台结构：月份、关键词、类型分段、更多筛选、活跃筛选 Chip、Owner CSV 导出和批量标签进入统一工具栏；筛选继续写入 URL，翻页和清除筛选保持既有查询参数语义。
- 桌面与移动：桌面使用日期、类型、分类/标题、付款人、范围/分摊、标签、金额和操作表格；移动只渲染卡片，明确显示归档分类、个人/共同、可见性、分摊、付款人和金额。
- 详情与动作：详情抽屉复用焦点圈定，保留复制一笔、存为模板、软删除确认、附件和承担明细；软删除与批量标签 mutation、权限和 query invalidation 未改变。
- 自动化结果：前端 18 个测试文件、66 个测试通过，lint 与 production build 通过；主包约 659 kB 的既有分包告警继续归属性能专项。
- 视觉证据：`docs/project_analysis/ui-fl-05-transactions-2026-07-14/` 覆盖 1440px 表格、390px 卡片、筛选 Sheet 和详情抽屉；CDP 记录 `innerWidth=scrollWidth=390`，桌面不显示移动列表，移动端不显示宽表。
- 本机部署：生产构建静态资源已更新到 `http://localhost:38088` 的 WSL staging，health 保持 `staging / schema 19 / db ok`；本次未重建后端镜像、未迁移或修改数据库。
- UI-FL-05E（2026-07-14）：已落地创建者/角色权限、原账单回填、差异 PATCH、类型锁定、归档元数据保留、普通附件编辑、共同支出 `equal/payer_only` 重算边界和离线禁用；后端同时修复元数据编辑重建 splits、普通编辑恢复归档标签的问题。
- UI-FL-05E 自动化：前端 18 个测试文件、70 个测试、lint 和 production build 通过；显式使用 Qt MinGW gcc 后 handler/transaction 测试通过，OpenAPI 解析通过。该阶段遗留的固定镜像与真实账号门禁已在 2026-07-15 最终运行收口中关闭。
- 详细契约：`docs/project_analysis/2026-07-14-ui-fl-05e-edit-contract.md`；波次 C 准备评审：`docs/project_analysis/2026-07-14-ui-fl-wave-c-readiness.md`。

最终运行收口（2026-07-15）：

- 本机 `ledger-two:1.2.0-rc-b83f766` 回读为 `staging / schema 19 / XLSX enabled / db ok`，更新前在线备份通过 `quick_check`。
- 真实 QA 账号在主实例完成 Fresh Light、流水列表、共同支出编辑器和 390/1440px 无横向溢出验收；主实例未提交账单修改。
- 隔离数据库副本完成普通元数据编辑与恢复、归档元数据保留、共同 split 保留、附件引用保留、批量标签、软删除和 CSV 导出写路径；原 staging 保持 40 条账单且未出现验收标签。
- 运行验收发现并修复编辑态残留“保存并继续”和移动记账按钮遮挡抽屉两个缺陷，提交为 `67253bc`、`b83f766`。
- 证据：`docs/project_analysis/ui-fl-05e-runtime-2026-07-15/`。UI-FL-05 最终门禁关闭。

### UI-FL-06：结算解释与确认

范围：范围切换、转账行动、paid/share/raw_net/settlement/final_net、复制文案和登记确认。<br>
完成标准：登记明确生成 settlement 且不改历史账单；复制不改变状态。<br>
验证：结算组件测试、复制失败兜底、真实登记路径、375/1440 截图。

实现与验收（2026-07-15）：

- 范围契约：`GET /api/settlements/balance` 保持全部账期兼容，新增可选 `month=YYYY-MM` 的真实月份聚合；共同支出、分摊和结算记录使用同一月份范围，非法月份返回 400。
- 页面结构：Fresh Light 结算页覆盖全部未结/仅本月、转账方向、实际支付/承担、共同支出净额、已登记结算、最终未结、影响账单和历史结算；最终金额继续直接消费服务端整数分 DTO。
- 可信操作：复制文案不写业务数据，Clipboard API 不可用或失败时展示可选择文本；确认框明确双方、金额、新增 settlement 记录且不修改历史共同支出，按钮使用“生成结算记录”。
- 缓存一致性：成功后同时失效 balance、settlements、dashboard、transactions 和 reports；交易表单保存共同支出时失效全部结算范围缓存。
- 自动化：显式使用 Qt MinGW gcc 的 `go test ./...` 通过；前端 20 个测试文件、78 个测试、lint 和 production build 通过。
- 运行验收：本机 `ledger-two:1.2.0-rc-ui-fl-06` 回读 `staging / schema 19 / XLSX enabled / db ok`；375/1440px 均无横向溢出，复制失败兜底和确认文案可见。
- 隔离写路径：数据库副本新增 1 条 settlement 和 1 条对应流水，余额从 13200 分归零；7 条共同支出的 ID、金额、状态和更新时间完全不变。原 staging 仍为 40 条流水、2 条结算且无验收记录。
- 证据：`docs/project_analysis/ui-fl-06-runtime-2026-07-15/`；实现提交 `daccda3`。UI-FL-06 门禁关闭，下一任务为 UI-FL-07。

### UI-FL-07：设置与元数据

范围：设置分区、分类/标签/账户、备份恢复、诊断和角色态。<br>
完成标准：owner/editor/viewer 边界不变；高风险动作说明影响并二次确认。<br>
验证：权限、归档恢复、备份诊断回归，375/1440 截图。

实现与验收（2026-07-15）：

- 信息架构：设置页重组为账本成员、分类与账户、规则与模板、导入导出、数据安全和系统诊断六个可定位分区；桌面使用职责说明与操作区双列布局，移动端使用两列分组入口和单列内容。
- 权限：所有成员可查看名单；Owner 管理成员和元数据，Owner/Editor 保留导出当前角色可见数据能力，Viewer 只读，备份/恢复准备/诊断保持 Owner-only；后端全量 RBAC 回归通过。
- 元数据：分类、标签、支付账户统一搜索、活跃/归档分段、顺序、引用数量、颜色、编辑、归档和恢复；归档确认明确历史名称保留。
- 数据安全：备份、导出、成员移除、角色调整和元数据归档复用共享确认组件；恢复流程明确“创建前置备份并生成停机人工指引”，不冒充在线恢复。
- 自动化：前端 22 个测试文件、82 个测试、lint 和 production build 通过；显式使用 Qt MinGW gcc 的 `go test ./...` 通过。
- 运行验收：本机 `ledger-two:1.2.0-rc-ui-fl-07` 回读 `staging / schema 19 / XLSX enabled / db ok`；设置和元数据 375/1440px 均无横向溢出，主实例保持 40 条流水、2 条结算、13 个活跃和 5 个归档分类。
- 证据：`docs/project_analysis/ui-fl-07-settings-metadata-2026-07-15/`；实现提交 `912523c`。UI-FL-07 门禁关闭，下一任务为 UI-FL-08。

### UI-FL-08：导入工作台

范围：Entry、Preview、移动卡片、Row Editor、Commit Confirm 和结果状态。<br>
完成标准：CSV/XLSX、new/duplicate/suspicious/invalid/adjusted 状态清楚；preview 不写正式账单。<br>
验证：Task47U/48U/49U/49XU 用例、真实脱敏文件 preview、375/390/1440 截图。

实现与验收（2026-07-15）：

- 信息架构：导入流程重组为来源与文件、Preview 批次、行级审阅、规则管理和提交结果；微信 CSV/XLSX、支付宝 CSV、通用 CSV 的支持矩阵在入口直接展示。
- 审阅状态：parser 摘要、总数、新增、重复、疑似、错误、跳过和待处理均有数字与文字；桌面复用 `ResponsiveDataList`，移动端只渲染卡片。
- 行级调整：Bottom Sheet 支持类型、分类、账户、标签和 `private/partner_readable`；当前 importer 不生成 splits，因此没有开放会制造错误领域语义的 `shared` 选项。
- 规则与提交：规则支持新增、编辑和归档；提交确认显示导入/跳过/疑似/错误数量并明确整批原子回滚，Preview 阶段明确不写入正式账单。
- 权限与响应式：设置入口和工作台保持 Owner-only；导入页移动端隐藏全局记账 FAB，避免和批次提交栏重叠；375/1440px 均无横向溢出。
- 自动化：前端 25 个测试文件、91 个测试、lint 和 production build 通过；显式使用 Qt MinGW gcc 的 `go test ./...` 通过。
- 运行验收：本机镜像 `ledger-two:1.2.0-rc-ui-fl-08` 回读 `staging / schema 19 / XLSX enabled / db ok`；匿名 fixture 预览后正式流水保持 40 条，未执行 commit，NAS 未更新。
- 证据：`docs/project_analysis/ui-fl-08-import-workbench-2026-07-15/`；实现提交 `c3399df`。UI-FL-08 门禁关闭，下一任务为 UI-FL-09。

### UI-FL-09：分析与钻取

范围：趋势、分类、成员、标签和到流水的筛选钻取。<br>
完成标准：图表有数据表述和空状态；成员展示支付、承担、净垫付；settlement 不进入消费统计。<br>
验证：统计口径回归、钻取筛选、375/1440 截图。

实现与验收（2026-07-15）：

- 趋势：复用既有 `monthly-summary` query key 组合当前账期前六个月数据，显示支出、收入、个人、共同和当前未结；没有新增 API 或前端金额口径。
- 分类/标签：排行使用可读进度、金额和占比；分类 ID 与标签名直接写入流水页已有筛选参数，多标签全额命中口径在页面显式说明。
- 成员：展示实际支付、消费承担、垫付净额、已付/已收结算与最终未结；成员支付钻取复用 `payer_user_id`，不展示 UUID 或“记账人排行”。
- 信任边界：页面说明统计基于当前用户可见账单，settlement 不进入消费统计；后端金额、可见性和结算算法保持权威。
- 响应式：1440px 使用五项摘要、双列趋势/口径与双成员卡；375px 使用两列摘要和单列内容，无横向溢出。
- 自动化：前端 27 个测试文件、99 个测试、lint 和 production build 通过；本会话 Go 全量测试已通过且本任务没有修改后端。
- 运行验收：本机镜像 `ledger-two:1.2.0-rc-ui-fl-09` 回读 `staging / schema 19 / XLSX enabled / db ok`；真实 QA 账号完成四 Tab、分类/成员/标签钻取和 375/1440px 验收，正式流水保持 40 条。
- 证据：`docs/project_analysis/ui-fl-09-analytics-2026-07-15/`；实现提交 `9d8066f`。UI-FL-09 门禁关闭后进入 UI-FL-10，当前 UI-FL-10 亦已完成。

### UI-FL-10：全局验收

范围：视觉一致性、可访问性、响应式、真实业务路径、Dark Glass 回退和证据归档。<br>
完成标准：P0/P1 缺陷关闭；375/390/430/1440 无重叠和横向滚动；关键流程均有证据。<br>
验证：前端完整门禁、浏览器截图/像素检查、登录到结算/导入的端到端回归。

实现与验收（2026-07-15）：

- 默认主题：无偏好新会话进入 Fresh Light；显式 Dark Glass/Fresh Light 选择均持久化并可回退。
- 可访问性：AppShell 增加 skip link 和路由主内容焦点；登录密码按钮、周期规则控件、草稿 BottomSheet/ConfirmDialog 具备语义与键盘路径。
- 响应式：登录、Dashboard、流水、分析、结算、设置、导入和周期规则在 375/390/430/1440 共 28 个路由视口组合均无横向溢出或可见元素越界。
- 自动化：前端 28 个测试文件、104 个测试、lint、production build 和 Go 全量测试通过；既有约 676 kB 主包告警转入 P2 性能专项。
- 运行环境：固定镜像 `ledger-two:1.2.0-rc-ui-fl-10` 回读 `staging / schema 19 / XLSX enabled / db ok`；验收前后数据库均为 `2|2|40|2|34` 且 `quick_check=ok`。
- 证据：`docs/project_analysis/ui-fl-10-global-2026-07-15/`；实现提交 `c8d63fe`。UI-FL-10 与 Fresh Light UI/UX 专项关闭，后续 UI 变更由对应业务 Task 复用本计划的组件和门禁。

## 11. 每个任务的交付模板

每个 UI-FL 任务开始前在任务记录中填写：

```text
关联业务 Task：
路由与页面：
共享组件归属：
预计修改文件：
API/DTO/权限/金额：保持不变 / 需另行评审
并行任务与冲突判断：
Figma/Frame/本地规范：
验收视口：
自动化测试：
真实业务路径：
回滚方式：
```

每个任务单独提交。若设计规范、代码和验收记录属于同一原子任务，可在该 Task 内分成“规范冻结”和“实现验收”两个提交，不应跨 Task 混合。

## 12. 验证门禁

基础前端门禁：

```bash
cd frontend
npm run lint
npm run test
npm run build
```

UI 验收至少覆盖：

- 375、390、430、1440 视口。
- 默认、加载、空、错误、离线、无权限和高风险确认。
- 键盘焦点、抽屉焦点返回、44x44 触控目标、文字对比和非颜色状态提示。
- 登录、记账、流水筛选、结算、元数据、导入 preview/commit 等受影响真实路径。

一旦 UI 改动触及业务行为，还必须执行对应后端、API、金额、权限、导入或迁移测试，不能只运行前端构建。

## 13. 风险与回滚

| 风险 | 控制 | 回滚 |
|---|---|---|
| 双主题造成样式分叉 | 先统一语义 Token，不复制页面代码 | 回退主题映射或当前 UI-FL 原子提交 |
| 并行任务修改同一组件 | 共享组件唯一归属、契约先提交 | 暂停页面任务并回到归属任务修复 |
| 视觉迁移改变业务口径 | API 不变声明、真实业务回归 | 回退页面提交，保留后端与 Dark Glass |
| Figma 与代码再次漂移 | 四个同步点、Frame/截图/任务反向登记 | 以已验收代码和高优先级事实源为准，记录设计差异 |
| 一次性改动过大 | 按 UI-FL 原子提交和波次推进 | 逐任务回退，不做整分支大回滚 |

## 14. 完成定义

UI 专项只有同时满足以下条件才可标记完成：

1. UI-FL-01 至 UI-FL-10 均有独立提交和验收记录。
2. 业务 Task、UI-FL、Figma handoff、实现和验收之间可双向追踪。
3. Fresh Light 成为默认体验前已完成 Dark Glass 回退验证。
4. 未引入未经评审的 API、migration 或业务范围。
5. 线上 Figma 未完成项继续明确标注，不以本地预览代替同步证明。
