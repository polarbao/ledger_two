# Task50：v1.3 多账本正式化开发前计划

状态：Task50P.1-P.4 已完成；P.5-P.6 待完成，尚未达到编码准入
创建日期：2026-07-15  
适用阶段：v1.2 发布门禁与 UI-FL-01 至 UI-FL-10 收口期间的并行文档准备；正式开发在两者完成后启动

## 1. 目标

把路线图中的“多账本正式化”从三条高层目标补齐为可冻结、可迁移、可验收的 v1.3 Task50。准备阶段只定义账本生命周期、成员角色、隔离、兼容和 UI 协同，不提前修改业务代码或 migration。

## 2. 当前结论

现有资料已完成现状盘点、产品范围、技术与 Migration、OpenAPI、Fixture 与验收冻结，但不足以直接编码。

已具备：

1. `ledgers`、`ledger_members`、owner/editor/viewer、LedgerContext 和统一 RolePolicy。
2. `X-Ledger-Id`、前端 active ledger、ledger-scoped query key 和切换失效机制。
3. 账本创建、列表、成员新增/改角色/移除的基础 API 和设置页入口。
4. 多账本隔离、viewer 拒绝写入、private/partner_readable/shared 的既有测试基线。
5. Task50 高层路线、v1.3 产品方向和 Fresh Light 共享组件规范。

缺失：

1. Task50 UI 流程与开发准入；PRD、技术实现、OpenAPI、Fixture 和验收矩阵已冻结。
2. 账本归档/恢复、成员管理、无账本和归档历史的完整交互状态。
3. 最后一个 owner、成员自移除、当前账本被归档、默认账本回退等边界。
4. `partner_readable` 与成员上限已冻结为双人语义；仍需将历史可见性警告落实到 API/UI/验收。
5. 多账本导入、导出、备份、恢复、周期规则、模板、附件和审计的隔离矩阵。
6. 与 UI-FL-07 设置页、UI-FL-10 全局收口之间的文件所有权和 Figma Frame。

因此，Task50 仍不进入代码阶段；下一步执行 P.5 UI/UX 评审，并在 UI-FL-10 完成后执行 P.6。

已完成事实源：

1. `docs/project_analysis/2026-07-15-task50-current-state-gap-matrix.md`：Task50P.1 现状与差距矩阵。
2. `docs/prd/31-prd-v1.3-multi-ledger.md`：Task50P.2 冻结 PRD。
3. `docs/tech/25-v1.3-multi-ledger-implementation-contract.md`：Task50P.3 技术与 Migration 冻结契约。
4. `docs/api/openapi-v1.3-ledger-draft.yaml`：Task50P.4 DTO、错误码、ETag/If-Match、生命周期和实例运维 API 契约。
5. `docs/prd/32-v1.3-task50-acceptance-fixtures.md`：Task50P.4 匿名 Fixture、跨账本隔离、RBAC、Migration 与 UI 验收矩阵。
6. PRD 已冻结最多两名成员、archived 默认隐藏且只读、唯一 Owner 原子移交、显式账本请求、无账本状态、历史可见性和 Owner/Editor 导出。
7. 技术契约已冻结 LifecyclePolicy、version/If-Match、Editor 导出、独立 `instance_admins`、实例审计与 fallback 清理。
8. migration 已冻结为 020 生命周期/成员约束与 021 实例管理员，production 禁止 goose down，schema 21 为目标门禁。

## 3. 推荐范围

Task50 建议只正式化“一个账号管理多个独立账本”的可信闭环：

1. 创建、切换、重命名、归档和恢复账本。
2. owner/editor/viewer 角色和成员管理不变量。
3. 所有业务数据、查询缓存、导入批次、导出和备份按账本隔离。
4. 当前账本失效后的明确回退和无账本空状态。
5. 归档账本默认只读；恢复后重新开放写入。

推荐 Task50 不增加多人分摊范围：

1. 不新增 ratio/shares/custom 分摊 UI。
2. 不新增最小转账建议算法。
3. 不实现公开邀请、链接邀请、邮件/短信/微信通知。
4. 不改变 settlement、历史共同账单和导入金额口径。
5. 新增第三名及以上成员是否开放，必须在 PRD 中单独冻结；推荐留到 Task51，避免成员数量先扩张而分摊与可见性语义没有闭环。

## 4. 必须产出的事实源

| 产物 | 建议路径 | 完成标准 |
|---|---|---|
| Task50 PRD | `docs/prd/31-prd-v1.3-multi-ledger.md` | 角色、场景、范围、不做项、生命周期、不变量和验收冻结 |
| 技术契约 | `docs/tech/25-v1.3-multi-ledger-implementation-contract.md` | 模块边界、数据模型、API、权限、缓存、审计和回滚可直接指导实现 |
| Migration 评审 | 技术契约独立章节或附录 | 只新增 migration；升级、降级、旧数据补值和索引方案明确 |
| UI 流程 | `docs/ui/16-v1.3-multi-ledger-flows.md` | 账本选择、创建、归档、恢复、角色态、空状态和移动端流程完整 |
| API 草案 | `docs/api/openapi-v1.3-ledger-draft.yaml` | DTO、错误码、幂等、分页、归档和成员管理契约可解析 |
| 验收矩阵 | PRD/测试文档中的 Task50 矩阵 | 跨账本隔离、RBAC、归档、缓存、附件、导入导出和回滚均有用例 |
| Figma handoff | `docs/ui/figma/` 对应 Task50 目录 | Frame、状态、组件复用和代码映射可追踪；本地审阅不冒充线上同步 |
| 详细任务卡 | 本文件冻结后升级或新增实现任务卡 | 每个原子 Task 有文件范围、依赖、测试、回滚和提交边界 |

## 5. 准备任务

### Task50P.1：现状与差距盘点

范围：代码、migration 007-010、LedgerContext、RBAC、active ledger、设置页和现有隔离测试。  
输出：能力矩阵、已实现/部分实现/缺失/历史兼容四类结论。  
完成标准：不把“已有入口”误判为“正式业务闭环”。

完成情况（2026-07-15）：已完成。输出 `docs/project_analysis/2026-07-15-task50-current-state-gap-matrix.md`，覆盖 lifecycle、成员不变量、LedgerContext fallback、前端状态、缓存、全模块隔离、导出/备份和 migration 风险。

### Task50P.2：产品范围冻结

必须冻结：

1. Task50 最大成员数及 Task51 边界。
2. 账本归档后是只读、隐藏还是二者组合。
3. 最后一个 owner、成员自移除、移交 owner 和删除账号的规则。
4. 当前账本被归档/失去成员资格时的回退。
5. private、partner_readable、shared 在目标成员数下的定义。

完成标准：PRD 不存在影响数据模型或权限矩阵的 Open Question。

完成情况（2026-07-15）：已完成。输出 `docs/prd/31-prd-v1.3-multi-ledger.md`；产品范围已冻结，技术实现形式已由 P.3 冻结，API/UI 表达继续由 P.4/P.5 评审。

### Task50P.3：技术与 Migration 评审

建议优先评审：

1. `ledgers` 增加 `status/archived_at/archived_by`，还是独立生命周期表。
2. 归档账本写入 Guard 放在 LedgerContext/RolePolicy，而不是散落 service。
3. 当前账本只存前端偏好还是增加服务端用户偏好。
4. 账本归档、成员变更和角色变化的审计事件。
5. 跨账本 transaction/import/attachment/template/recurring/settlement 查询的索引和测试。

完成标准：新增 migration 编号、升级前后数据守恒、回滚边界和 schema 门禁冻结。

完成情况（2026-07-15）：已完成。输出 `docs/tech/25-v1.3-multi-ledger-implementation-contract.md`；冻结 lifecycle model、统一 archived Guard、显式 LedgerContext、实例管理员、migration 020/021、schema 21、升级预检和前滚优先回滚策略。

### Task50P.4：API、Fixture 与验收冻结

范围：账本生命周期、成员管理、错误码、并发冲突、空账本状态和跨账本访问。  
完成标准：OpenAPI 可解析；owner/editor/viewer 与非成员用例完整；A 账本对象 ID 不能在 B 账本读取、修改、导出或下载附件。

完成情况（2026-07-15）：已完成。输出 `docs/api/openapi-v1.3-ledger-draft.yaml` 与 `docs/prd/32-v1.3-task50-acceptance-fixtures.md`；冻结 path/header mismatch、ETag/If-Match、实例管理员、ready batch discard、正常/异常 Fixture、全模块隔离和 schema 19 -> 21 守恒用例。草案明确不是当前 router 已实现接口。

### Task50P.5：UI/UX 与 UI-FL 协同

文件所有权：

1. UI-FL-07 只迁移现有设置和元数据能力，不提前实现 Task50 新生命周期。
2. UI-FL-10 负责全局主题、响应式和可访问性收口，不新增 Task50 业务规则。
3. Task50 拥有账本选择器的新状态、账本管理页、归档/恢复流程和无账本状态。
4. Task50 必须复用 UI-FL-01/02 的 Button、Dialog、Sheet、AppShell 和断点，不创建平行组件。

完成标准：375/390/430/1440 Frame、owner/editor/viewer、加载/空/错/归档/无权限状态齐全。

### Task50P.6：开发准入评审

同时满足后才允许 Task50.1 开始：

1. v1.2 NAS schema 19 与 production 发布门禁关闭，或明确书面决定 Task50 仅在独立开发环境推进。
2. UI-FL-10 完成；至少 UI-FL-07 的设置页组件和文件所有权已经冻结。
3. Task50 PRD、Tech、UI、OpenAPI、migration 和验收矩阵均为“已冻结”。
4. 开发、staging、production 数据库继续物理隔离。
5. 工作树无未归属的真实数据库、账单文件、密钥或 Figma 原始包。

## 6. 预计实现切片

以下切片只用于估算，准备冻结前不得直接编码：

| Task | 目标 | 主要风险 |
|---|---|---|
| Task50.1 | 冻结 lifecycle model、migration 和 repository | 旧账本补值、索引、回滚 |
| Task50.2 | 统一归档账本 LedgerContext/RolePolicy Guard | 散落 fallback、越权写入 |
| Task50.3 | 账本生命周期与成员不变量 API | 最后 owner、并发角色变更 |
| Task50.4 | active ledger 回退、Query cache 与 AppShell 状态 | 串账、旧缓存、无账本循环 |
| Task50.5 | Fresh Light 账本管理与归档/恢复 UI | UI-FL-07 文件冲突、移动端高风险操作 |
| Task50.6 | 全模块隔离、升级、回滚和真实路径验收 | 导入/附件/导出/备份遗漏 |

## 7. 风险与回滚

| 风险 | 控制 | 回滚 |
|---|---|---|
| 多成员语义提前泄漏 | Task50 冻结成员上限，Task51 独立评审 | 关闭新增第三成员入口，不回退既有 RBAC 基础 |
| 归档账本仍可写 | 统一 LedgerContext Guard 与跨模块测试 | 回退应用镜像；migration 必须可恢复旧 status |
| 切换账本串缓存 | 所有 query key 含 ledgerId，切换后定向失效 | 回退前端提交，不修改数据库 |
| UI-FL 与 Task50 冲突 | 文件所有权和契约先冻结 | UI-FL 只保留视觉迁移，Task50 延后页面实现 |
| v1.2 数据受影响 | 环境物理隔离和 schema 备份 | 独立回滚 Task50 development 数据库与镜像 |

## 8. 当前下一步

1. 执行 Task50P.5，冻结多账本 UI 流程、角色态、危险确认和 Fresh Light handoff；Task50 代码继续禁入。
2. UI-FL-09 已完成，UI 主线进入 UI-FL-10 全局验收；只有 UI-FL 全部结束并通过 P.6 才允许 Task50.1 开始。
3. P.4 已完成，后续实现必须逐项绑定 `T50-*` 用例，不能重新发明错误码或 Fixture。
4. Task49X 继续按发布计划关闭 NAS 门禁，不与 Task50 migration 混合。
