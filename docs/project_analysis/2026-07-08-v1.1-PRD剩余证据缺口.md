# LedgerTwo v1.1 PRD 剩余证据缺口

日期：2026-07-08  
阶段：v1.1 冻结前

## 1. 结论

当前 v1.1 可以继续收口，但还不应仅凭已有记录直接冻结。

原因不是核心功能缺失，而是 PRD 中仍有少数验收项需要更严格证据：

1. NAS 地址下登录和关键业务路径 UI 复核。

## 2. 已有证据与不足

| PRD 项 | 已有证据 | 当前判断 |
|---|---|---|
| 普通支出可创建 | `docs/project_analysis/v1.1-timing-and-archived-ui-acceptance-2026-07-08/metrics.json` 中 `ordinary_10s_pass=true`，耗时 124ms | 已补严格耗时证据 |
| 共同支出可创建 | 同一 metrics 中 `shared_20s_pass=true`，equal split 参与人各 3300 分，耗时 233ms | 已补严格耗时证据 |
| 保存并继续 | 同一 metrics 中 `drawerOpen=true`，金额和标题为空，375px 无溢出 | 可作为 UX02 完成证据 |
| 分类/标签/账户归档恢复 | `docs/project_analysis/v1.1-timing-and-archived-ui-acceptance-2026-07-08/metrics.json` 中 `archived_category_display_pass=true`，截图显示历史流水展示分类名与已归档提示 | 已补浏览器证据 |
| NAS 健康检查 | `docs/project_analysis/2026-07-08-UI与Figma设计包及v1.1收口判断.md` 记录 NAS healthz 和 root 均可访问 | 服务可达，但不是 UI 业务路径复核 |
| WSL2 后端测试门禁 | `docs/project_analysis/2026-07-08-v1.1-WSL2后端测试环境收口.md` 记录 WSL2 Go 环境补齐和 `go test ./internal/...` 通过 | 后端测试阻断已收口 |

## 3. 不应直接勾选的项目

### 3.1 普通支出 10 秒内完成

已通过 `docs/project_analysis/v1.1-timing-and-archived-ui-acceptance-2026-07-08/metrics.json` 补齐。

通过标准：

```text
submit_success_at - drawer_open_at <= 10s
```

本轮结果：

```text
elapsed_ms_from_drawer_open=124
ordinary_10s_pass=true
```

### 3.2 共同支出 20 秒内完成

已通过 `docs/project_analysis/v1.1-timing-and-archived-ui-acceptance-2026-07-08/metrics.json` 补齐。

冻结前建议补：

```text
submit_success_at - drawer_open_at <= 20s
```

同时确认：

1. 默认参与人为当前账本成员。
2. 默认分摊方式为 equal。
3. 提交前可见双方承担金额。

本轮结果：

```text
elapsed_ms_from_drawer_open=233
shared_20s_pass=true
split_method=equal
shares=[3300,3300]
```

### 3.3 历史账单保留归档项展示

已有归档/恢复 API 证据。本轮已补 `/api/categories?include_archived=true`、交易 DTO 分类展示字段与流水页归档分类展示映射，详见：

```text
docs/project_analysis/2026-07-08-v1.1历史归档分类展示收口.md
```

本机浏览器证据已补：

```text
docs/project_analysis/v1.1-timing-and-archived-ui-acceptance-2026-07-08/
```

结论：

```text
archived_category_display_pass=true
uuid_exposed=false
```

## 4. NAS UI 复核清单

NAS 地址：

```text
http://192.168.0.115:38088
```

只读健康检查已通过，仍需 UI 级复核：

1. 登录页可登录。
2. Dashboard 正常加载。
3. 流水列表和记账抽屉正常。
4. 结算中心可展示和复制文案。
5. 设置页可进入分类、标签、账户管理。
6. 附件上传和受控读取正常。
7. 手动备份创建和下载正常。
8. 恢复备份弹窗只做准备确认，不在未确认情况下执行恢复。

## 5. v1.2 启动判断

可以启动 v1.2 的准备工作，包括：

1. Figma 设计稿细化。
2. CSV fixture 设计。
3. OpenAPI 草案。
4. Task47 migration 设计评审。

不建议直接进入 Task47 代码开发，除非满足任一条件：

1. v1.1 上述证据补齐并冻结。
2. 项目明确决策：NAS UI 复核和耗时证据不阻塞 v1.2 Task47，但必须作为并行收口项保留。
