# v1.2 Task49 开发与收口准备记录

日期：2026-07-09

## 1. 当前阶段

v1.2 已进入 Task49 导入规则阶段。

已完成：

- Task47：CSV 导入预览、行级调整、移动端预览工作台。
- Task48：导入批次 commit、事务落库、去重映射、审计日志、提交确认与结果反馈已完成；I02/I03/I04、375px/390px 和失败反馈证据见 `docs/project_analysis/v1.2-task48-task49-acceptance-2026-07-09/`。
- Task49 后端：导入规则 create/list/update/archive/restore。
- Task49 预览：active 规则按 priority 命中预览行，写入 `suggested_*`、`suggested_rule_id`、`suggestion_reason`，不覆盖 `selected_*`。
- Task49U 基础：导入工作台内置规则管理面板，支持创建、归档、恢复，预览行展示命中解释。
- Task49U 增强：规则编辑、active/archived/all 筛选、多标签推荐选择、命中解释展示推荐分类/账户/标签名称已落地。
- Task49U 归档提示：规则列表已对引用归档或不可用元数据的规则显示处理提示；新建/编辑表单仅展示启用元数据，避免继续选择归档项。
- Figma 配套：当前仓库有 Figma 建模输入包，Task49 补充规格已落到 `docs/ui/figma/v1.2-task49-import-rule-manager-handoff.md`；当前账户主文件已同步至 https://www.figma.com/design/Q4m7LRw75qrkFdw4O5xmU0，截图验收记录见 `docs/project_analysis/v1.2-task49-figma-sync-2026-07-09/`。原文件 https://www.figma.com/design/wkU5RRZs5R7McjNUlEaFF2 保留为历史参考。

## 2. 剩余任务

### Task49 验收

以下项目已完成，证据见 `docs/project_analysis/v1.2-task48-task49-acceptance-2026-07-09/`：

- active 规则命中，archived 规则不命中。
- 规则引用归档元数据时不应用建议。
- 用户手工调整后不被规则覆盖。
- viewer/editor 后端拒绝规则读取和管理。
- 390px 规则管理面板、命中解释无横向滚动。

## 3. 冻结条件

v1.2 可冻结需要满足：

1. 后端 `go test ./internal/db ./internal/importer ./internal/http/router ./internal/http/handler` 通过。
2. 前端 `npm run build` 通过。
3. Task48/49 的移动端截图或 CDP 指标存在。
4. OpenAPI 与 API inventory 覆盖 `/api/imports/*` 和 `/api/import-rules/*`。
5. 导入规则只提供建议，不自动提交，不绕过用户确认。

## 4. 下一步建议

优先顺序：

1. v1.2 冻结前综合测试。
2. OpenAPI 与 API inventory 最终状态同步。
3. migration 013-018 顺序和空库升级复核。
4. v1.2 发布候选状态与剩余风险记录。
