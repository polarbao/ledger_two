# NAS-R1 双环境重建与真实数据入口记录

日期：2026-07-20<br>
状态：NAS-R1.1 至 NAS-R1.5、NAS-R1.7 完成；38088 已初始化，NAS-R1.6 仅待用户确认两个账号登录<br>
结论：38088 已重建并初始化为发布/真实体验 production，38092 已重建为未初始化的开发联调/验收 staging；production 基线已保存到 NAS 内外

## 1. Environment result

| Item | 38088 production | 38092 staging |
|---|---|---|
| LAN | `http://192.168.0.115:38088` | `http://192.168.0.115:38092` |
| public HTTP | `http://nas.polarrrr.top:38088` | `http://nas.polarrrr.top:38092` |
| runtime root | `/volume1/docker/ledger-two` | `/volume1/docker/ledger-two-development` |
| container | `ledger-two-v13-production` | `ledger-two-v13-development` |
| image | `ledger-two:1.3.0-rc-task53-98c3b14` | `ledger-two:1.3.0-rc-task53-98c3b14` |
| channel/schema/mode | production / 22 / suggest | staging / 22 / suggest |
| XLSX/db | enabled / ok | enabled / ok |
| init status | true | false |
| runtime/env/db mode | 0700 / 0600 / 0600 | 0700 / 0600 / 0600 |

两个环境使用独立 runtime、数据库、secret 和容器。38088 后续保存真实数据；38092 只保存匿名 Fixture 或 QA 数据，可以按任务重建。

## 2. Pre-reset backup

旧 38088 在停止写入后保存到 NAS 外：

```text
E:\__Project_Data\ledger_two_nas_backups\pre-real-experience-20260720-170801
```

校验结果：

| Asset | SHA-256 |
|---|---|
| `ledger-schema18.db` | `466d137d21666c25c10d98889edf8408cd2cbe34cddfed5ac11cd344b4d13ed5` |
| `production-config.tar.gz` | `c258490d7125e02b4d5817d3337ef661548d9068ae74d09f6f921a332f02ea8e` |

SQLite `PRAGMA quick_check` 返回 `ok`，`foreign_key_check` 无结果。备份业务基线为 schema 18、2 users、1 ledger、7 transactions、1 settlement。该备份不进入 Git，销毁前需要再次确认。

## 3. Cleanup and deployment

1. 删除旧 38088 runtime 活跃数据，并使用新的 production secret 重建空 schema 22 数据库。
2. 删除旧 38089 容器和 `/volume1/docker/ledger-two-staging`，端口 38089 已关闭。
3. 删除旧 `/volume1/docker/ledger-two-task53-staging`，在 `/volume1/docker/ledger-two-development` 重建 38092。
4. 删除带回车符的历史误建目录。
5. NAS 端临时 pre-reset 副本已删除；NAS 外备份继续保留。
6. 部署包目录已收紧为 0700；production/development runtime 为 root:root 0700，`.env` 和数据库为 0600。

## 4. Verification

1. LAN 38088：部署后 `init=false`；用户操作后复验为 `1.3.0-rc / production / schema 22 / suggest / db ok / init=true`。
2. LAN 38092：`1.3.0-rc / staging / schema 22 / suggest / db ok / init=false`。
3. 38089 无服务；旧 staging、旧 Task53 runtime、误建目录和 NAS 端 pre-reset 副本均不存在。
4. 容器分别映射 `38088->8080` 和 `38092->8080`，均使用固定 Task53 候选镜像。
5. 公网重建后复验：38088 为 5/5 HTTP 200；38092 首轮 4/5 HTTP 200、1 个区域节点超时，立即复验为 5/5 HTTP 200。

公网复验报告：

- 38088：`https://check-host.net/check-report/44ea46edka3b`
- 38092：`https://check-host.net/check-report/44ea49b3kb54`

公网当前仅为明文 HTTP。以上验证只证明 DNS、端口转发和无凭据 health 可达，不允许通过公网执行初始化、登录、账单访问或文件上传。

## 5. Production baseline

发现 38088 变为 `initialized=true` 后，立即使用 SQLite 在线 `.backup` 建立首份真实 production 基线：

| Item | Result |
|---|---|
| backup id | `nas-r1-real-experience-baseline-20260720-180104` |
| NAS copy | `/volume1/docker/ledger-two/backups/baseline/nas-r1-real-experience-baseline-20260720-180104` |
| off-NAS copy | `E:\__Project_Data\ledger_two_nas_backups\real-experience-baselines\nas-r1-real-experience-baseline-20260720-180104` |
| database SHA-256 | `cb32a9eb33672c9247749283bd9588c4a7f46b7098def02c340e5754c8f3ce5d` |
| archive SHA-256 | `b0dd7c67e5edd7c7ed0394c7800b367d6fbc86b65dd23661408682534249d8ed` |
| integrity | quick_check=ok / foreign_key_check=ok / schema 22 |

基线业务不变量为 2 users、2 ledgers、3 ledger_members、38 categories、16 tags，交易、split、settlement 和导入批次均为 0。归档内包含恢复所需的 production 配置，因此必须按凭据级敏感文件保管，不得进入 Git 或发送到公开位置。

## 6. User action and remaining gate

NAS-R1.6 的初始化状态已由接口确认，仍需用户完成最后的人工登录验收：

1. 只从局域网访问 `http://192.168.0.115:38088`。
2. 验证两个真实账号都能登录；凭据不进入聊天、命令、文档或日志。
3. 确认默认分类/标签可见，账本没有历史演示交易。
4. 向维护者确认登录验收结果，关闭 NAS-R1.6。

NAS-R1.7 已完成。38088 从此禁止再次清库；后续版本只允许保留原库的向前 migration 和成对回滚，每次升级前仍需创建新的 NAS 内外一致性备份。
