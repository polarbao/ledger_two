# LedgerTwo v1.2.0-rc 发布候选验收清单

更新时间：2026-07-10

当前结论：本机发布门禁已通过；NAS 升级未执行

## 1. 本机发布门禁

| 项目 | 命令或证据 | 状态 |
|---|---|---|
| 工作树范围检查 | `git status --short`、`git diff --check` | 通过，只有本轮发布文档与入口更新 |
| 后端全量测试 | `cd backend && go test ./... -count=1` | 通过 |
| 前端 lint | `cd frontend && pnpm lint` | 通过 |
| 前端测试 | `cd frontend && pnpm test` | 通过，6 files / 19 tests |
| 前端构建 | `cd frontend && pnpm build` | 通过，保留 652.40 kB 主 chunk 警告 |
| Compose 配置 | `docker compose config --quiet` | 通过 |
| Docker 构建 | `docker build -t ledger-two:rc-check -f deploy/docker/Dockerfile .` | 通过 |
| 本机健康检查 | `/api/healthz` 为 `1.2.0-rc` / schema 18 | 通过，容器 healthy |
| 环境隔离 | health、诊断、UI、Compose 和 NAS 双目录约束 | 本地实现与 375px UI 验收通过 |
| OpenAPI | 主契约与导入补充契约可解析、path 参数一致 | 已通过，见冻结记录 |
| 移动端导入验收 | Task47U/48U/49U 证据完整 | 已通过，见冻结记录 |

## 2. NAS 发布窗口

| 项目 | 状态 |
|---|---|
| 确认维护窗口和回滚负责人 | 未执行 |
| 下载应用手动备份并保存 NAS 外副本 | 一致性快照已保存到 NAS 外本机目录，SHA-256 一致 |
| 备份数据库、附件、环境变量和 Compose | 已完成：`backups/predeploy/v1.2.0-rc-20260710-120459/` |
| 记录旧镜像 ID/提交号和旧 health | 部分完成：旧 health 为 `1.1.0-rc` / schema 12 |
| 独立 staging 目录、端口和数据库 | 已准备：`ledger-two-staging` / 38089 / 独立 schema 12 副本 |
| 同步候选代码或固定标签镜像 | 提交 `0fbe0dc` 代码包已校验；等待 sudo 构建启动 |
| staging 升级到 schema 18 | 已通过，production 仍保持 schema 12 |
| staging health、登录、历史核心数据验收 | 已通过；附件目录沿用备份快照，待 production 最终抽检 |
| staging 导入预览与规则 API | 已通过：通用 CSV 4 行预览、规则读取成功，未提交正式账单 |
| staging 升级后备份 | 已通过，生成 352256 字节手动备份 |
| staging 容器重启验收 | 待交互式 sudo 执行 |

本机 WSL2 的 Docker 普通用户当前无 `/var/run/docker.sock` 权限，本轮使用免密 `sudo` 完成镜像构建与容器状态检查；该环境差异不影响镜像产物，但后续应单独整理本机 Docker 用户组权限。当前 Compose 缺少 buildx，构建验证使用 legacy builder 完成，属于工具链提示，不阻断候选版本。

NAS production 备份验证：源数据库与备份均通过 `PRAGMA quick_check`；升级前 schema 为 12，核心数量为用户 2、账本 1、账单 7、结算 1。外部备份保存于本机 `nas_management_docs/ledger_two_backups/`，未进入 Git 仓库。

## 3. 放行规则

1. 本机门禁必须全部通过，非阻断警告要有明确记录。
2. NAS 项目全部完成前，版本保持 `1.2.0-rc`，不得宣称稳定版发布。
3. 阻断缺陷必须修复并重新执行受影响门禁。
4. 主 chunk 大小提示和全产品 Figma 覆盖不足当前按非阻断风险管理。
5. NAS 部署不在未经确认的维护窗口自动执行。
